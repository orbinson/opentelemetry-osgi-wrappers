{
  "id": "${project.groupId}:${project.artifactId}:slingfeature:opentelemetry-semconv:${project.version}",
  "bundles": [
    {
      "id": "${project.groupId}:opentelemetry-semconv:1.27.0-alpha.0001-SNAPSHOT",
      "start-order": 5
    }
  ]
}
